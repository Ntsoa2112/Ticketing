<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tableau de bord</title>
</head>
<body>
  <h1>Tableau de bord</h1>
  <p>Matricule: <%= session.User.matricule %></p>
  <p>Catégorie: <%= session.User.categorie %></p>
  
  
  <a href="/logout">Se déconnecter</a><br>
  <% if(session.User.categorie != 'trans'){ %>
    <a href="/demander_a_trans">Faire une demande</a>
  <% } %>
</body>
</html>
